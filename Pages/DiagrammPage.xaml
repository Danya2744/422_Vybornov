<Page x:Class="_422_Vybornov.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_422_Vybornov.Pages"
      d:DesignHeight="450" d:DesignWidth="900"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d" 
      Title="Диаграммы">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <TextBlock Text="Пользователь:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CmbUser" DisplayMemberPath="FIO" Width="200" Margin="5" 
                     SelectionChanged="UpdateChart" ToolTip="Выберите пользователя"/>

            <TextBlock Text="Тип диаграммы:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CmbDiagram" Width="150" Margin="5" 
                     SelectionChanged="UpdateChart" ToolTip="Выберите тип диаграммы"/>

            <Button x:Name="BtnExportExcel" Content="Экспорт в Excel" 
                   Click="BtnExportExcel_Click" Margin="5" Padding="10,5"/>
            <Button x:Name="BtnExportWord" Content="Экспорт в Word" 
                   Click="BtnExportWord_Click" Margin="5" Padding="10,5"/>
        </StackPanel>
        <WindowsFormsHost HorizontalAlignment="Left" Height="361" Margin="0,10,0,0" Grid.Row="1" VerticalAlignment="Top" Width="883">
            <charts:Chart x:Name="ChartPayments">
                <charts:Chart.Legends>
                    <charts:Legend/>
                </charts:Chart.Legends>
            </charts:Chart>
        </WindowsFormsHost>
    </Grid>
</Page>
